@model IEnumerable<app.Models.Alarm>

@{
<<<<<<< HEAD:app/Views/Admin/Alarms.cshtml
    ViewData["Title"] = "Alarmer";
    ViewData.AddActivePage(ManageNavPages.Alarms);
}

=======
    ViewData["Title"] = Localizer["Alarms"];
}

<h2>@Localizer["Alarms"]</h2>

>>>>>>> Rebaser til master:app/Views/Alarms/Index.cshtml
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                Verktøy
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @item.Tool.Name
            </td>
            <td class="fit">
                <div class="dropdown">
                    <button id="more-btn" class="btn btn-primary dropdown-toggle btn-sm" type="button" data-toggle="dropdown">
                        <span class="more-dot"></span>
                        <span class="more-dot"></span>
                        <span class="more-dot"></span>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-right">
                        <li><a href="/Alarms/Edit/@item.Id">Edit</a></li>
                        <li><a href="/Alarms/Details/@item.Id">Details</a></li>
                        <li><a href="/Alarms/Delete/@item.Id">Delete</a></li>
                    </ul>
                </div>
            </td>
        </tr>
    }
    @if (Model.Count() == 0)
    {
        <tr>
            <td colspan="3">Ingen alarmer å vise...</td>
        </tr>
    }
    </tbody>
</table>
